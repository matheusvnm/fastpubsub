"""TOTEST:
1. On startup/shutdown/after_shutdown/after_startup hooks
2. Create app with callable hooks
3. start_hooks/shutdown_hooks
4. API Routes (probes).
5. Run (start/end) -> mock the broker (start/stop)
"""
